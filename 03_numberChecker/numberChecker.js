function numberChecker(number) {
  // if (number === 6) {  // original
  // if (number === 1000) {  // 1 exercise
  // if (number === 10) { // 2 exercise
  // if (number === 7) { // 3 exercise
  // if (number === 7) { // 4 exercise
  if (number === 7) {
    return true;
  } else {
    return false;
  }
}

// Do not edit below this line
module.exports = numberChecker;
